/*
 * SPDX-FileCopyrightText: 2006-2021 Istituto Italiano di Tecnologia (IIT)
 * SPDX-License-Identifier: BSD-3-Clause
 */

// Autogenerated by Thrift Compiler (0.14.1-yarped)
//
// This is an automatically generated file.
// It could get re-generated if the ALLOW_IDL_GENERATION flag is on.

#include <BipedalLocomotion/YarpUtilities/VectorsCollection.h>

namespace BipedalLocomotion::YarpUtilities {

// Constructor with field values
VectorsCollection::VectorsCollection(const std::map<std::string, std::vector<double>>& vectors) :
        WirePortable(),
        vectors(vectors)
{
}

// Read structure on a Wire
bool VectorsCollection::read(yarp::os::idl::WireReader& reader)
{
    if (!read_vectors(reader)) {
        return false;
    }
    if (reader.isError()) {
        return false;
    }
    return true;
}

// Read structure on a Connection
bool VectorsCollection::read(yarp::os::ConnectionReader& connection)
{
    yarp::os::idl::WireReader reader(connection);
    if (!reader.readListHeader(1)) {
        return false;
    }
    if (!read(reader)) {
        return false;
    }
    return true;
}

// Write structure on a Wire
bool VectorsCollection::write(const yarp::os::idl::WireWriter& writer) const
{
    if (!write_vectors(writer)) {
        return false;
    }
    if (writer.isError()) {
        return false;
    }
    return true;
}

// Write structure on a Connection
bool VectorsCollection::write(yarp::os::ConnectionWriter& connection) const
{
    yarp::os::idl::WireWriter writer(connection);
    if (!writer.writeListHeader(1)) {
        return false;
    }
    if (!write(writer)) {
        return false;
    }
    return true;
}

// Convert to a printable string
std::string VectorsCollection::toString() const
{
    yarp::os::Bottle b;
    if (!yarp::os::Portable::copyPortable(*this, b)) {
        return {};
    }
    return b.toString();
}

// read vectors field
bool VectorsCollection::read_vectors(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    size_t _csize;
    yarp::os::idl::WireState _ktype;
    yarp::os::idl::WireState _vtype;
    reader.readMapBegin(_ktype, _vtype, _csize);
    for (size_t _i = 0; _i < _csize; ++_i) {
        size_t _msize;
        yarp::os::idl::WireState _lst;
        reader.readListBegin(_lst, _msize);
        std::string _key;
        if (reader.noMore()) {
            reader.fail();
            return false;
        }
        if (!reader.readString(_key)) {
            reader.fail();
            return false;
        }
        std::vector<double>& _val = vectors[_key];
        if (reader.noMore()) {
            reader.fail();
            return false;
        }
        size_t _csize;
        yarp::os::idl::WireState _etype;
        reader.readListBegin(_etype, _csize);
        // WireReader removes BOTTLE_TAG_LIST from the tag
        constexpr int expected_tag = ((BOTTLE_TAG_FLOAT64) & (~BOTTLE_TAG_LIST));
        if constexpr (expected_tag != 0) {
            if (_csize != 0 && _etype.code != expected_tag) {
                return false;
            }
        }
        _val.resize(_csize);
        if (_csize != 0 && !reader.readBlock(reinterpret_cast<char*>(_val.data()), _val.size() * sizeof(double))) {
            return false;
        }
        reader.readListEnd();
        reader.readListEnd();
    }
    reader.readMapEnd();
    return true;
}

// write vectors field
bool VectorsCollection::write_vectors(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeMapBegin(BOTTLE_TAG_STRING, BOTTLE_TAG_LIST | BOTTLE_TAG_FLOAT64, vectors.size())) {
        return false;
    }
    for (const auto& _item : vectors) {
        if (!writer.writeListBegin(0,2)) {
            return false;
        }
        if (!writer.writeString(_item.first)) {
            return false;
        }
        if (!writer.writeListBegin(BOTTLE_TAG_FLOAT64, _item.second.size())) {
            return false;
        }
        if (!writer.writeBlock(reinterpret_cast<const char*>(_item.second.data()), _item.second.size() * sizeof(double))) {
            return false;
        }
        if (!writer.writeListEnd()) {
            return false;
        }
        if (!writer.writeListEnd()) {
            return false;
        }
    }
    if (!writer.writeMapEnd()) {
        return false;
    }
    return true;
}

// read (nested) vectors field
bool VectorsCollection::nested_read_vectors(yarp::os::idl::WireReader& reader)
{
    if (reader.noMore()) {
        reader.fail();
        return false;
    }
    size_t _csize;
    yarp::os::idl::WireState _ktype;
    yarp::os::idl::WireState _vtype;
    reader.readMapBegin(_ktype, _vtype, _csize);
    for (size_t _i = 0; _i < _csize; ++_i) {
        size_t _msize;
        yarp::os::idl::WireState _lst;
        reader.readListBegin(_lst, _msize);
        std::string _key;
        if (reader.noMore()) {
            reader.fail();
            return false;
        }
        if (!reader.readString(_key)) {
            reader.fail();
            return false;
        }
        std::vector<double>& _val = vectors[_key];
        if (reader.noMore()) {
            reader.fail();
            return false;
        }
        size_t _csize;
        yarp::os::idl::WireState _etype;
        reader.readListBegin(_etype, _csize);
        // WireReader removes BOTTLE_TAG_LIST from the tag
        constexpr int expected_tag = ((BOTTLE_TAG_FLOAT64) & (~BOTTLE_TAG_LIST));
        if constexpr (expected_tag != 0) {
            if (_csize != 0 && _etype.code != expected_tag) {
                return false;
            }
        }
        _val.resize(_csize);
        if (_csize != 0 && !reader.readBlock(reinterpret_cast<char*>(_val.data()), _val.size() * sizeof(double))) {
            return false;
        }
        reader.readListEnd();
        reader.readListEnd();
    }
    reader.readMapEnd();
    return true;
}

// write (nested) vectors field
bool VectorsCollection::nested_write_vectors(const yarp::os::idl::WireWriter& writer) const
{
    if (!writer.writeMapBegin(BOTTLE_TAG_STRING, BOTTLE_TAG_LIST | BOTTLE_TAG_FLOAT64, vectors.size())) {
        return false;
    }
    for (const auto& _item : vectors) {
        if (!writer.writeListBegin(0,2)) {
            return false;
        }
        if (!writer.writeString(_item.first)) {
            return false;
        }
        if (!writer.writeListBegin(BOTTLE_TAG_FLOAT64, _item.second.size())) {
            return false;
        }
        if (!writer.writeBlock(reinterpret_cast<const char*>(_item.second.data()), _item.second.size() * sizeof(double))) {
            return false;
        }
        if (!writer.writeListEnd()) {
            return false;
        }
        if (!writer.writeListEnd()) {
            return false;
        }
    }
    if (!writer.writeMapEnd()) {
        return false;
    }
    return true;
}

} // namespace BipedalLocomotion::YarpUtilities
